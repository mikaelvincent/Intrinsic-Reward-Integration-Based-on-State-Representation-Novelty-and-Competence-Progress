\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/eval_learning_curves.png}
\caption{
Learning curves on the benchmark suite (Table~\ref{tab:env_suite}). Curves show the mean evaluation return across seeds at each saved checkpoint; semi-transparent points show the corresponding per-seed returns.
Evaluation uses 20 episodes per checkpoint with the deterministic policy mode (Section~\ref{sec:experimental_setup:evaluation}); higher return is better.
Dashed horizontal lines mark fixed ``solved'' score thresholds used for reference (MountainCar: $-110$, BipedalWalker: $300$, HalfCheetah: $6000$, Ant: $5000$, Humanoid: $3000$).
}
\label{fig:eval_learning_curves}
\end{figure*}
